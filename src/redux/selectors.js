export const selectKamps = (state) => state.kamps.kamps;
export const selectIsLoading = (state) => state.kamps.isLoading;
export const selectLoadedItems = (state) => state.kamps.loadedItems;
export const selectCurrentPage = (state) => state.kamps.currentPage;
export const selectFavoriteKamp = (state) => state.favorites.favorites;
